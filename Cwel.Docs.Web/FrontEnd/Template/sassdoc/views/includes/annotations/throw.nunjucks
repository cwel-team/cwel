{% if item.throw.length > 0 %}
<tr>
    <th>
        <h4 class="sd-meta__heading">Throws</h4>
    </th>
    <td>
        <ul>
            {% for _throw in item.throw %}
                <li>{{ _throw|safe }}</li>
            {% endfor %}
        </ul>
    </td>
</tr>
{% endif %}
