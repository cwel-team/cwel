@model Cwel.Docs.Web.Models.PlaygroundPage
@{
    Layout = Model.Layout;
}

@foreach (var item in Model.Children)
{
    var itemModel = item.GetModel();
    @Html.Partial(itemModel.View, itemModel.Model)
}
