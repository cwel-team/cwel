@using Cwel.Web
@model {{ name | pascalize }}ViewModel

@{
    Html.RequireScript(AssetType.Component, "{{ name | pascalize }}");
}

<div class="{{ name | kebabize }}">
    <h1 class="{{ name | kebabize }}__title">Greeting</h1>
    <p class="{{ name | kebabize }}__text">Hello World</p>
</div>
