# http://galenframework.com/docs/reference-galen-test-suite-syntax/

@@ table breakpoints
    | breakpointName  | tags      | size     |
    | XS              | xs        | 320x800  |
    | S               | s         | 450x800  |
    | M               | m         | 650x800  |
    | L               | l         | 1150x800 |
    | XL              | xl        | 1450x800 |

@@ set
    base_url         http://docs.cwel.local/visualtest/
    spec_path        Cwel/Src/Test/visual/specs/

@@ parameterized using breakpoints
Grid at breakpoint: ${breakpointName}
    ${base_url}grid ${size}
        check ${spec_path}cwel-grid.gspec --include '${tags}'

@@ parameterized using breakpoints
REM Units at breakpoint: ${breakpointName}
    ${base_url}remunits ${size}
        check ${spec_path}cwel-remunits.gspec --include '${tags}'
