# http://galenframework.com/docs/reference-galen-test-suite-syntax/

@@ table devices
    | deviceName     | tags       | gridArgs |
    | Chrome 62      | xl         | --size 1280x800 --browser chrome --dc.browser_version 62 |
    | Firefox 39     | xl         | --size 1280x800 --browser firefox --dc.browser_version 39 --dc.accept_insecure_certs true |
    | IE 11          | xl         | --size 1280x800 --browser 'internet explorer' --dc.browser_version 11 |

@@ set
    host_base_url       ${host_url}/visualtest/
    spec_path           Cwel/Src/Test/visual/specs/
    gridArgsForAll     --dc.build '${buildName}' --dc.browserstack.local true --dc.browserstack.debug true

@@ parameterized using devices
Grid on: ${deviceName}
    selenium grid ${grid_url} --page ${host_base_url}grid ${gridArgs} ${gridArgsForAll}
        check ${spec_path}cwel-grid.gspec --include '${tags}'

@@ parameterized using devices
Rem units on: ${deviceName}
    selenium grid ${grid_url} --page ${host_base_url}remunits ${gridArgs} ${gridArgsForAll}
        check ${spec_path}cwel-remunits.gspec --include '${tags}'
